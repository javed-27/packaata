data structure 
[].length = 52
{color : '', symbol : '', value : '' }

shuffling => jsr package

least count :
decks : 2
max Players : 5/deck
cards : 7

give random cards to players
display cards by sorting

display ui :
at bottom --> cards
middle --> deck , joker.
left --> previous
right --> thrown card .

game play
 ->  cards(decks) 
 -> shuffle splice(0,7) per person
 -> remaing deck 
 -> start game
 -> open card, joker.
 -> first player
 -> cards will be dipslay from left to right 
 -> user will select which cards to drop the relaese button
 -> previous cards will be store in an array and the last card will be display on the deck
 -> show button if palyer want to click the show button the player score should be less than 10
 -> after that every player score should be caluculated and display the winner

function(value,group,color) => create card

